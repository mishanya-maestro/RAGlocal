# 📚 Сборник нормативных правовых актов Республики Беларусь

Ниже представлен перечень ключевых законодательных документов, используемых в работе.

| Код файла | 📄 Название документа |
|:----------|:----------------------|
| `IZKrb.txt` | Избирательный кодекс Республики Беларусь |
| `KONrb.txt` | Конституция Республики Беларусь |
| `OGOrb.txt` | Закон «Об основах гражданского общества» |
| `OMMrb.txt` | Закон «О массовых мероприятиях» |
| `ONSrb.txt` | Закон «О Национальном собрании Республики Беларусь» |
| `OOGrb.txt` | Закон «Об обращениях граждан и юридических лиц» |
| `OOOrb.txt` | Закон «Об общественных объединениях» |
| `OPPrb.txt` | Закон «О политических партиях» |
| `OVNrb.txt` | Закон «О Всебелорусском народном собрании» |
| `SMIrb.txt` | Закон «О средствах массовой информации» |
| `SNGrb.txt` | Закон «О присоединении Республики Беларусь к Конвенции о стандартах демократических выборов, избирательных прав и свобод в государствах – участниках Содружества Независимых Государств» |

---

> 📌 *Актуальность документации рекомендуется проверять по официальным источникам.*

---

## Web UI (Flask)

Установка зависимостей (в venv проекта):

```bash
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

Запуск сайта (важно: тот же Python, куда ставили пакеты):

```bash
run.bat
```

или:

```bash
.\.venv\Scripts\python.exe src/flask_app.py
```

Не используйте голый `py -3 src/flask_app.py`, если пакеты ставили только в `.venv` —
тогда Flask не увидит `faster-whisper` (диск C: часто переполнен, и поставить пакет
в системный Python не получается).

Открыть в браузере:

```text
http://127.0.0.1:5000
```

Чат всегда работает в обычном режиме: ответ через RAG (поиск + контекст статей).

### 🎙️ Голосовой ввод (STT)

В композере есть кнопка микрофона: первое нажатие начинает запись, второе —
останавливает и распознаёт речь локально через Whisper (`faster-whisper`)
или через AssemblyAI, если задан ключ.

Кэш моделей Whisper хранится в `.cache/huggingface` внутри проекта (диск D:),
чтобы не упираться в полный диск C:.

Опционально — ключ AssemblyAI в `.env`:

```text
ASSEMBLYAI_API_KEY=ваш_ключ
```

Голосовая запись работает только по `localhost` или по HTTPS (требование
браузера для `getUserMedia`).

### Анализ документов на соответствие законодательству

Вкладка **Анализ документа** (`/analyze`) позволяет загрузить PDF, DOCX или TXT и получить проверку на соответствие действующему законодательству РБ.

Что делает система:
- классифицирует документ (договор, исковое заявление, приказ, трудовой договор);
- ищет релевантные статьи в нужных кодексах;
- проверяет обязательные условия по чек-листам;
- выводит замечания со степенью риска, цитатами из документа и нормы, а также предложениями по правке;
- позволяет фильтровать по категории и экспортировать отчёт в Markdown.

Юридический корпус проекта расширен placeholder-актами для демонстрации архитектуры:
- Гражданский кодекс РБ (`data/GKrb.txt`) — договоры;
- Трудовой кодекс РБ (`data/TKrb.txt`) — трудовые договоры и приказы;
- Гражданский процессуальный кодекс РБ (`data/GPKrb.txt`) — исковые заявления;
- Закон о защите прав потребителей (`data/ZPPPT.txt`) — потребительские договоры.

Для production-качества эти файлы необходимо заменить на официальные тексты с Национального правового интернет-портала РБ, после чего перестроить индексы:

```bash
.\.venv\Scripts\python.exe src\generate_fulltext.py
.\.venv\Scripts\python.exe src\ingest.py
```
